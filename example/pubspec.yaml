name: bg_location_tracker_example
description: "Example app for bg_location_tracker"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  bg_location_tracker:
    path: ../
  permission_handler: ^12.0.1
  toast: ^0.3.0
  flutter_map: ^8.2.2
  latlong2: ^0.9.1
  flutter_background_geolocation: ^5.0.4
  flutter_local_notifications: ^17.2.4
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
